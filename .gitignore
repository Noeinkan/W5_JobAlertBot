node_modules/
.env
data/*.db
data/*.db-*
logs/
*.log

# RTK (Rust Token Killer) — local-only Copilot integration
.github/copilot-instructions.md
.github/hooks/

# Local scratch / ad-hoc files (queries, debug dumps, one-off notes)
.scratch/
